---
phase: 01-core-foundation--content-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.mjs
  - next.config.ts
  - .gitignore
  - .env.example
  - .env.local
autonomous: true

must_haves:
  truths:
    - "Next.js development server starts without errors"
    - "TypeScript compilation succeeds"
    - "Tailwind CSS utilities are available"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next@"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "compilerOptions"
    - path: "tailwind.config.ts"
      provides: "Tailwind CSS configuration with serif font"
      contains: "fontFamily"
    - path: "next.config.ts"
      provides: "Next.js configuration with Unsplash domain"
      contains: "images"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "dependencies"
---

<objective>
Initialize a production-ready Next.js 16 project with TypeScript, Tailwind CSS, and all dependencies required for Phase 1.

Purpose: Establishes the foundation for all subsequent work. Clean setup prevents configuration issues later.
Output: Fully configured Next.js project ready for feature development.
</objective>

<execution_context>
@/Users/krzysiekprzekwas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/krzysiekprzekwas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation--content-display/01-RESEARCH.md
@.planning/phases/01-core-foundation--content-display/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js project with TypeScript</name>
  <files>
package.json
tsconfig.json
next.config.ts
.gitignore
  </files>
  <action>
Initialize Next.js 16 project using create-next-app with TypeScript and App Router:

```bash
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*" --use-npm --yes
```

This creates:
- package.json with Next.js 16.x dependencies
- tsconfig.json with App Router paths configured
- next.config.ts for configuration
- .gitignore with standard Node.js patterns

Update next.config.ts to allow Unsplash images:
```typescript
import type { NextConfig } from 'next';

const config: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
    ],
  },
};

export default config;
```
  </action>
  <verify>
Run `npm run build` to verify Next.js compiles without errors. Check that next.config.ts includes Unsplash domain in remotePatterns.
  </verify>
  <done>
Next.js project initialized with TypeScript, App Router enabled, and Unsplash images whitelisted in next.config.ts.
  </done>
</task>

<task type="auto">
  <name>Install all Phase 1 dependencies</name>
  <files>
package.json
  </files>
  <action>
Install all libraries needed for Phase 1 based on research recommendations:

```bash
npm install unsplash-js date-fns
npm install -D @types/node
```

Dependencies installed:
- **unsplash-js**: Official Unsplash API client (type-safe, preserves ixid)
- **date-fns**: Date manipulation for deterministic quote selection
- **@types/node**: TypeScript types for Node.js APIs

Note: sharp and @vercel/og are NOT needed as dependencies - they're built into Next.js App Router for /api/og routes.

Do NOT install:
- puppeteer (causes cold start timeouts - use Satori instead)
- canvas (not serverless-friendly)
- axios (use native fetch)
  </action>
  <verify>
Run `npm list unsplash-js date-fns` to confirm packages installed. Check package.json dependencies section includes both packages.
  </verify>
  <done>
All Phase 1 dependencies installed: unsplash-js, date-fns. Package.json shows correct versions.
  </done>
</task>

<task type="auto">
  <name>Configure Tailwind with serif font and zen aesthetic</name>
  <files>
tailwind.config.ts
app/layout.tsx
  </files>
  <action>
Update tailwind.config.ts to extend theme with serif font family and zen/meditative spacing:

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      fontFamily: {
        serif: ['var(--font-serif)', 'Georgia', 'serif'],
      },
    },
  },
  plugins: [],
};

export default config;
```

Update app/layout.tsx to load Playfair Display (elegant serif) using next/font/google:

```typescript
import type { Metadata } from 'next';
import { Playfair_Display } from 'next/font/google';
import './globals.css';

const playfair = Playfair_Display({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-serif',
  weight: ['400', '700'],
});

export const metadata: Metadata = {
  title: 'Daily Demotivations',
  description: 'Your daily dose of demotivating quotes on beautiful landscapes',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={playfair.variable}>
      <body className="antialiased">{children}</body>
    </html>
  );
}
```

Why Playfair Display: Elegant, readable serif font that contrasts with demotivating content (per user decision in CONTEXT.md).
  </action>
  <verify>
Run `npm run dev` and check that homepage loads with serif font applied. Inspect computed styles in browser - font-family should show Playfair Display.
  </verify>
  <done>
Tailwind configured with serif font family. Layout.tsx loads Playfair Display via next/font/google with CSS variable --font-serif.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run dev` - server starts on port 3000 without errors
2. Visit http://localhost:3000 - page loads with Next.js default content
3. Check app/layout.tsx - Playfair Display font is loaded
4. Check next.config.ts - images.unsplash.com is whitelisted
5. Run `npm run build` - production build succeeds
6. Check package.json - all dependencies present (next, react, unsplash-js, date-fns, tailwindcss)
</verification>

<success_criteria>
- Next.js development server starts without errors
- TypeScript compilation passes with zero errors
- Tailwind CSS classes available and functioning
- Playfair Display font loaded and ready to use
- All Phase 1 dependencies installed
- Unsplash images domain whitelisted in Next.js config
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation--content-display/01-01-SUMMARY.md`
</output>
