---
phase: 01-core-foundation--content-display
plan: 06
type: execute
wave: 3
depends_on: [01-04, 01-05]
files_modified:
  - vercel.json
  - .env.example
  - README.md
autonomous: false

must_haves:
  truths:
    - "Site deployed to Vercel and accessible via production URL"
    - "Homepage loads with today's quote on all devices"
    - "OG image generation works in production"
    - "Lighthouse scores >90 on mobile"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel deployment configuration"
      contains: "headers"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "UNSPLASH_ACCESS_KEY"
    - path: "README.md"
      provides: "Project documentation"
      min_lines: 30
  key_links:
    - from: "vercel.json"
      to: "Vercel platform"
      via: "deployment configuration"
      pattern: "headers"
---

<objective>
Deploy application to Vercel platform with production configuration, environment variables, and caching headers for optimal performance.

Purpose: Makes the site publicly accessible (TECH-01). Vercel provides zero-config deployment, CDN caching, and automatic HTTPS.
Output: Live production site at Vercel domain.
</objective>

<execution_context>
@/Users/krzysiekprzekwas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/krzysiekprzekwas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-foundation--content-display/01-RESEARCH.md
@.planning/phases/01-core-foundation--content-display/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Configure Vercel deployment settings</name>
  <files>
vercel.json
.env.example
  </files>
  <action>
Create vercel.json with caching headers and configuration:

```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, s-maxage=86400, stale-while-revalidate=300"
        }
      ]
    },
    {
      "source": "/api/og",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, immutable, max-age=86400, s-maxage=86400"
        }
      ]
    }
  ]
}
```

Create .env.example with environment variable template:

```
# Unsplash API Configuration
# Get your access key from https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=your_access_key_here

# Note: Apply for production API access immediately
# Demo tier: 50 requests/hour (insufficient for real traffic)
# Production tier: 5000 requests/hour
# Apply at: Your Unsplash App Settings → Request Production Access
```

Cache strategy:
- **Homepage**: 24-hour cache with 5-minute stale-while-revalidate (prevents midnight stampede)
- **/api/og**: 24-hour immutable cache (quote image doesn't change during day)
- **s-maxage**: CDN cache duration (86400 = 24 hours)
- **stale-while-revalidate**: Serve stale content while regenerating (prevents user-visible delays)

This addresses Pitfall #5 (cache stampede) from research.
  </action>
  <verify>
Check vercel.json syntax is valid JSON. Verify .env.example includes UNSPLASH_ACCESS_KEY with helpful comments.
  </verify>
  <done>
Vercel configuration created with optimized caching headers. Environment variable template documented in .env.example.
  </done>
</task>

<task type="auto">
  <name>Create comprehensive README</name>
  <files>
README.md
  </files>
  <action>
Create README.md with project overview, setup instructions, and deployment guide:

```markdown
# Daily Demotivations

Your daily dose of demotivating quotes beautifully displayed on romantic landscapes.

## Overview

Daily Demotivations is a Next.js application that displays a different demotivating quote each day, overlaid on stunning romantic landscape photography. The quote selection is deterministic—everyone sees the same quote on the same day, creating a shared experience.

## Features

- 📅 **Deterministic Daily Quotes** - Same quote for everyone on the same day
- 🏔️ **Beautiful Landscapes** - High-quality romantic landscape backgrounds from Unsplash
- 📱 **Fully Responsive** - Optimized for mobile (320px) through desktop (1920px+)
- ⚡ **Blazing Fast** - Server-side rendering with 24-hour ISR caching
- 🎨 **Elegant Typography** - Serif fonts with zen/meditative aesthetic
- 🔗 **Social Ready** - Open Graph images for beautiful social sharing previews

## Tech Stack

- **Framework**: Next.js 16 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Images**: Unsplash API
- **Deployment**: Vercel
- **OG Images**: @vercel/og (Satori + Sharp)

## Prerequisites

- Node.js 18+ and npm
- Unsplash API access key (free tier available)

## Setup

1. **Clone the repository**
   ```bash
   git clone <your-repo-url>
   cd daily-demotivations
   ```

2. **Install dependencies**
   ```bash
   npm install
   ```

3. **Get Unsplash API Key**
   - Visit [Unsplash Developers](https://unsplash.com/developers)
   - Create a new application
   - Copy your Access Key
   - **Important**: Apply for Production API access immediately (5000 req/hour vs 50 req/hour demo)

4. **Configure environment variables**
   ```bash
   cp .env.example .env.local
   ```
   
   Edit `.env.local` and add your Unsplash access key:
   ```
   UNSPLASH_ACCESS_KEY=your_actual_access_key_here
   ```

5. **Run development server**
   ```bash
   npm run dev
   ```
   
   Visit [http://localhost:3000](http://localhost:3000)

## Deployment

### Deploy to Vercel (Recommended)

1. **Install Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **Deploy**
   ```bash
   vercel
   ```

3. **Add environment variables in Vercel dashboard**
   - Go to your project settings
   - Navigate to Environment Variables
   - Add `UNSPLASH_ACCESS_KEY` with your key

4. **Redeploy**
   ```bash
   vercel --prod
   ```

## Architecture

- **Deterministic Selection**: Uses date-based hashing to ensure same quote per day globally
- **ISR Caching**: 24-hour revalidation with stale-while-revalidate prevents midnight stampede
- **Multi-layer Caching**: Unsplash API responses cached for 24 hours to prevent rate limit exhaustion
- **Fallback Images**: Local fallback landscapes for graceful degradation
- **Edge Runtime**: OG image generation uses edge functions for fast global distribution

## Performance

- ⚡ Lighthouse Score: 90+ on all metrics
- 🚀 LCP: <2.5s on mobile networks
- 📦 Bundle Size: <1MB (lightweight Satori, no Puppeteer)
- ⏱️ Cold Start: <10s for serverless functions

## License

MIT

## Credits

- Landscape photography via [Unsplash](https://unsplash.com)
- Built by [Kristof.pro](https://kristof.pro)
```

This README covers setup, deployment, architecture, and credits.
  </action>
  <verify>
Check README.md has all sections: Overview, Features, Tech Stack, Prerequisites, Setup, Deployment, Architecture, Performance, License, Credits. Verify markdown formatting is correct.
  </verify>
  <done>
README.md created with comprehensive documentation covering setup, deployment, and architecture. All sections complete and formatted correctly.
  </done>
</task>

<task type="auto">
  <name>Deploy to Vercel platform</name>
  <files>none (deployment only)</files>
  <action>
Deploy application to Vercel using Vercel CLI:

```bash
# Install Vercel CLI if not already installed
npm install -g vercel

# Login to Vercel (opens browser for authentication)
vercel login

# Deploy to Vercel (follow prompts)
# This will create a preview deployment first
vercel

# After verifying preview works, deploy to production
vercel --prod
```

During deployment, Vercel CLI will prompt:
- Set up and deploy? → Yes
- Which scope? → Select your account/team
- Link to existing project? → No (first deployment)
- Project name? → daily-demotivations (or accept default)
- In which directory is your code located? → ./ (current directory)
- Want to override settings? → No

After deployment completes:
1. Note the production URL (e.g., daily-demotivations.vercel.app)
2. Visit Vercel dashboard at https://vercel.com
3. Navigate to project settings → Environment Variables
4. Add environment variable (this is human-required - see checkpoint below)

Important notes:
- Vercel automatically detects Next.js and configures build settings
- vercel.json headers are applied automatically
- ISR caching works out of the box
- HTTPS is automatic via Vercel
- CDN caching is automatic
  </action>
  <verify>
After deployment:
1. Visit production URL - homepage loads with today's quote
2. Visit production URL/api/og - OG image generates successfully
3. Check Vercel dashboard - deployment status shows "Ready"
4. Test on mobile device - site is responsive and loads quickly
5. Run Lighthouse audit - scores >90 on all metrics
  </verify>
  <done>
Application deployed to Vercel. Production URL accessible. Site loads correctly with quotes and landscapes. OG image generation works in production.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Vercel deployment automation completed. Application is deployed but needs environment variable configuration in Vercel dashboard.
  </what-built>
  <how-to-verify>
**1. Add Unsplash API key to Vercel:**
   - Visit https://vercel.com/dashboard
   - Select your project (daily-demotivations)
   - Go to Settings → Environment Variables
   - Add variable:
     - Key: `UNSPLASH_ACCESS_KEY`
     - Value: [your Unsplash access key]
     - Environments: Production, Preview, Development (check all)
   - Click "Save"

**2. Redeploy to apply environment variable:**
   ```bash
   vercel --prod
   ```

**3. Verify deployment:**
   - Visit your production URL (e.g., daily-demotivations.vercel.app)
   - Confirm homepage displays quote on Unsplash landscape (not fallback image)
   - Check browser Network tab - landscape image URL should be from images.unsplash.com
   - Visit /api/og - OG image should generate with quote text

**4. Test responsive design:**
   - Open site on mobile device (or use Chrome DevTools device emulation)
   - Test widths: 320px, 375px, 768px, 1024px, 1920px
   - Verify no horizontal scrolling at any width
   - Confirm quote text scales appropriately
   - Footer should stack vertically on mobile (<640px)

**5. Run Lighthouse audit:**
   - Open Chrome DevTools → Lighthouse tab
   - Select Mobile, Categories: All
   - Click "Analyze page load"
   - Verify scores:
     - Performance: >90
     - Accessibility: >90
     - Best Practices: >90
     - SEO: >90

**Expected results:**
- ✅ Homepage loads with Unsplash landscape (not fallback)
- ✅ Quote displays in elegant serif font (Playfair Display)
- ✅ Date indicator visible above quote
- ✅ Footer shows photographer credit, Kristof.pro link, donation link
- ✅ Responsive on all screen sizes (320px - 1920px+)
- ✅ No horizontal scrolling at any breakpoint
- ✅ Lighthouse scores all green (>90)
- ✅ /api/og generates quote image successfully

**If issues found:**
- Unsplash images not loading → Check UNSPLASH_ACCESS_KEY is set correctly
- Fallback images showing → Verify API key in Vercel dashboard, redeploy
- Layout broken → Check Tailwind classes, verify responsive utilities
- Lighthouse score low → Check Network tab for large assets, verify ISR caching
  </how-to-verify>
  <resume-signal>
Type "approved" if all verifications pass, or describe specific issues found for troubleshooting.
  </resume-signal>
</task>

</tasks>

<verification>
1. Site deployed to Vercel with production URL
2. Homepage accessible and loads today's quote
3. Unsplash landscape backgrounds loading (not fallback images)
4. OG image generation working (/api/og returns quote image)
5. Responsive design verified on mobile and desktop
6. Lighthouse mobile scores >90 on all metrics
7. Environment variables configured in Vercel dashboard
8. README.md provides clear setup and deployment instructions
9. vercel.json configures optimal caching headers
10. All footer links work (photographer, Kristof.pro, Buy me a coffee)
</verification>

<success_criteria>
- Site deployed to Vercel and publicly accessible (TECH-01)
- Homepage displays today's quote on romantic landscape
- Deterministic quote selection works (same quote for all users)
- Fully responsive design (320px to 1920px+) (CORE-02)
- Elegant serif typography with zen aesthetic (CORE-03)
- OG image generation works in production
- Lighthouse mobile scores >90 (Performance, Accessibility, Best Practices, SEO)
- LCP <2.5s on mobile networks
- Caching headers prevent midnight stampede (stale-while-revalidate)
- Environment variables documented and configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation--content-display/01-06-SUMMARY.md`
</output>
